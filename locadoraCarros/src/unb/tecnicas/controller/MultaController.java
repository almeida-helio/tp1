package unb.tecnicas.controller;

import unb.tecnicas.model.Multa;

public interface MultaController {

    void createMulta(Multa multa);

    void updateMulta(Multa multa);

    void deleteMulta(Multa multa);
}
